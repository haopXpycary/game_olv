dict()
    key[]
    value[]
    __setitem__()
    __getitem__()
    __delitem__()
    clear()
    keys()   -> key
    values() -> value
cprint(int x,int y,str other,constName.color color,constName.color bgcolor)

screenOutput()
    int x|y = control.MaxScrX|MaxScrY
    [[[char pic,constName.color color,flag=Flush|Unflush]*x]*y] screen|nscreen
    update(int x,int y,char pic,constName.color color,Flush|Unflush flag)
    show()
    updateShow()
    
keyboardListen(threading.Thread)
    using:
    '''
    >>> get = keyboardListen()
    >>> get.start()
    >>> while True:
    ...     ch = get.ch
    ...     get.ch = False # or constName.Null
    '''
clear()

event()
    triggerFunc -> func()
    bool touched
    touch()
    untouch()